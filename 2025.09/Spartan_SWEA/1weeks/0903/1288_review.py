import sys

sys.stdin = open('test.txt', 'r')

'''
1288. 새로운 불면증 치료법
출처 : https://buly.kr/FsJ5tJC
'''

'''
    입력 예시
    5
    1  
    2
    11
    1295
    1692

    문제 요약
    - N의 배수를 하나씩 보면서 각 자리수 숫자를 기록한다.
    - 0~9 모든 숫자를 최소 한 번씩 본 시점의 배수값을 출력한다.
'''

T = int(input())  # 테스트 케이스 개수 입력

for t in range(1, T + 1):  # 각 테스트 케이스 반복
    N = int(input())  # 기준 수 N 입력

    # 0~9 숫자를 본 여부 저장하는 배열
    # (처음엔 모두 False 상태)

    # 지금까지 본 서로 다른 숫자 개수 카운트 변수

    # 배수 인덱스(k), 마지막 배수 값 저장 변수

    # 0~9 모든 숫자를 다 볼 때까지 반복
    # 배수 인덱스 증가
    # 현재 배수 값 계산
    # 마지막 배수 값 갱신

    # 현재 배수 값의 자릿수를 하나씩 확인
    # 문자 → 정수 변환
    # 아직 본 적 없는 숫자라면
    # 본 것으로 표시
    # 카운트 증가
    # 10개 다 봤으면 자릿수 순회 중단

    # 출력 형식: #테스트케이스번호 마지막배수값
